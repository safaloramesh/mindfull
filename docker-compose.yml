
version: '3.8'

services:
  app:
    build: .
    container_name: mindful_remind_app
    ports:
      - "3000:3000"
    environment:
      - API_KEY=${API_KEY}
      - NODE_ENV=production
    volumes:
      - mindful_data:/app/data
    restart: unless-stopped

volumes:
  mindful_data:
